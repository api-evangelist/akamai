naftiko: 1.0.0-alpha2
info:
  label: 'Akamai: Application Security API — Activation status'
  description: 'Akamai: Application Security API — Activation status. 2 operations. Lead operation: Akamai Get an activation request status. Self-contained Naftiko capability covering
    one Akamai business surface.'
  tags:
  - Akamai
  - Application Security
  - Activation status
  created: '2026-05-22'
  modified: '2026-05-22'
binds:
- namespace: env
  keys:
    AKAMAI_API_KEY: AKAMAI_API_KEY
capability:
  consumes:
  - type: http
    namespace: appsec-activation-status
    baseUri: https://{hostname}/appsec/v1
    description: 'Akamai: Application Security API — Activation status business capability. Self-contained, no shared references.'
    resources:
    - name: activations-status-statusId
      path: /activations/status/{statusId}
      operations:
      - name: getget-activations-status
        method: GET
        description: Get an activation request status
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: accountSwitchKey
          in: query
          type: string
          description: For customers who manage more than one account, this [runs the operation from another account](https://techdocs.akamai.com/developer/docs/manage-many-accounts-with-one-api-client).
            The Identity and
    - name: activations-activationId
      path: /activations/{activationId}
      operations:
      - name: getget-activation
        method: GET
        description: Get activation status
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: accountSwitchKey
          in: query
          type: string
          description: For customers who manage more than one account, this [runs the operation from another account](https://techdocs.akamai.com/developer/docs/manage-many-accounts-with-one-api-client).
            The Identity and
