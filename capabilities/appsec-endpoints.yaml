naftiko: 1.0.0-alpha2
info:
  label: 'Akamai: Application Security API — Endpoints'
  description: 'Akamai: Application Security API — Endpoints. 3 operations. Lead operation: Akamai Modify an API''s visibility. Self-contained Naftiko capability covering one Akamai
    business surface.'
  tags:
  - Akamai
  - Application Security
  - Endpoints
  created: '2026-05-22'
  modified: '2026-05-22'
binds:
- namespace: env
  keys:
    AKAMAI_API_KEY: AKAMAI_API_KEY
capability:
  consumes:
  - type: http
    namespace: appsec-endpoints
    baseUri: https://{hostname}/appsec/v1
    description: 'Akamai: Application Security API — Endpoints business capability. Self-contained, no shared references.'
    resources:
    - name: api-discovery-host-hostname-basepath-basePath
      path: /api-discovery/host/{hostname}/basepath/{basePath}
      operations:
      - name: putput-api-visibility
        method: PUT
        description: Modify an API's visibility
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: accountSwitchKey
          in: query
          type: string
          description: For customers who manage more than one account, this [runs the operation from another account](https://techdocs.akamai.com/developer/docs/manage-many-accounts-with-one-api-client).
            The Identity and
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: api-discovery-host-hostname-basepath-basePath-endpoints
      path: /api-discovery/host/{hostname}/basepath/{basePath}/endpoints
      operations:
      - name: postpost-api-endpoint-resource
        method: POST
        description: Create an endpoint or resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: accountSwitchKey
          in: query
          type: string
          description: For customers who manage more than one account, this [runs the operation from another account](https://techdocs.akamai.com/developer/docs/manage-many-accounts-with-one-api-client).
            The Identity and
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: getget-discovered-api-endpoints
        method: GET
        description: List discovered API endpoints
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: accountSwitchKey
          in: query
          type: string
          description: For customers who manage more than one account, this [runs the operation from another account](https://techdocs.akamai.com/developer/docs/manage-many-accounts-with-one-api-client).
            The Identity and
