naftiko: 1.0.0-alpha2
info:
  label: 'Akamai: Application Security API — Contracts and groups'
  description: 'Akamai: Application Security API — Contracts and groups. 1 operations. Lead operation: Akamai List contracts and groups. Self-contained Naftiko capability covering one
    Akamai business surface.'
  tags:
  - Akamai
  - Application Security
  - Contracts and groups
  created: '2026-05-22'
  modified: '2026-05-22'
binds:
- namespace: env
  keys:
    AKAMAI_API_KEY: AKAMAI_API_KEY
capability:
  consumes:
  - type: http
    namespace: appsec-contracts-and-groups
    baseUri: https://{hostname}/appsec/v1
    description: 'Akamai: Application Security API — Contracts and groups business capability. Self-contained, no shared references.'
    resources:
    - name: contracts-groups
      path: /contracts-groups
      operations:
      - name: getget-contracts-groups
        method: GET
        description: List contracts and groups
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: accountSwitchKey
          in: query
          type: string
          description: For customers who manage more than one account, this [runs the operation from another account](https://techdocs.akamai.com/developer/docs/manage-many-accounts-with-one-api-client).
            The Identity and
