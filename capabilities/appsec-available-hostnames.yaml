naftiko: 1.0.0-alpha2
info:
  label: 'Akamai: Application Security API — Available hostnames'
  description: 'Akamai: Application Security API — Available hostnames. 1 operations. Lead operation: Akamai List available hostnames for a new configuration. Self-contained Naftiko
    capability covering one Akamai business surface.'
  tags:
  - Akamai
  - Application Security
  - Available hostnames
  created: '2026-05-22'
  modified: '2026-05-22'
binds:
- namespace: env
  keys:
    AKAMAI_API_KEY: AKAMAI_API_KEY
capability:
  consumes:
  - type: http
    namespace: appsec-available-hostnames
    baseUri: https://{hostname}/appsec/v1
    description: 'Akamai: Application Security API — Available hostnames business capability. Self-contained, no shared references.'
    resources:
    - name: contracts-contractId-groups-groupId-selectable-hostnames
      path: /contracts/{contractId}/groups/{groupId}/selectable-hostnames
      operations:
      - name: getget-selectable-hostnames-per-config
        method: GET
        description: List available hostnames for a new configuration
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: accountSwitchKey
          in: query
          type: string
          description: For customers who manage more than one account, this [runs the operation from another account](https://techdocs.akamai.com/developer/docs/manage-many-accounts-with-one-api-client).
            The Identity and
