naftiko: 1.0.0-alpha2
info:
  label: 'Akamai: CP Codes and Reporting Groups API — Watermark limits'
  description: 'Akamai: CP Codes and Reporting Groups API — Watermark limits. 2 operations. Lead operation: Akamai Get a water-mark limit. Self-contained Naftiko capability covering
    one Akamai business surface.'
  tags:
  - Akamai
  - CP Codes
  - Watermark limits
  created: '2026-05-22'
  modified: '2026-05-22'
binds:
- namespace: env
  keys:
    AKAMAI_API_KEY: AKAMAI_API_KEY
capability:
  consumes:
  - type: http
    namespace: cp-codes-watermark-limits
    baseUri: https://{hostname}/cprg/v1
    description: 'Akamai: CP Codes and Reporting Groups API — Watermark limits business capability. Self-contained, no shared references.'
    resources:
    - name: cpcodes-contracts-contractId-watermark-limits
      path: /cpcodes/contracts/{contractId}/watermark-limits
      operations:
      - name: getget-cpcodes-watermark-limits
        method: GET
        description: Get a water-mark limit
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: accountSwitchKey
          in: query
          type: string
          description: For customers who manage more than one account, this [runs the operation from another account](https://techdocs.akamai.com/developer/docs/manage-many-accounts-with-one-api-client).
            The Identity and
    - name: reporting-groups-contracts-contractId-watermark-limits
      path: /reporting-groups/contracts/{contractId}/watermark-limits
      operations:
      - name: getget-reporting-groups-watermark-limits
        method: GET
        description: Get a water-mark limit for reporting groups
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: accountSwitchKey
          in: query
          type: string
          description: For customers who manage more than one account, this [runs the operation from another account](https://techdocs.akamai.com/developer/docs/manage-many-accounts-with-one-api-client).
            The Identity and
