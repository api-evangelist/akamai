naftiko: 1.0.0-alpha2
info:
  label: 'Akamai: Application Security API — Subscriptions'
  description: 'Akamai: Application Security API — Subscriptions. 2 operations. Lead operation: Akamai Subscribe or unsubscribe to recommendation emails. Self-contained Naftiko capability
    covering one Akamai business surface.'
  tags:
  - Akamai
  - Application Security
  - Subscriptions
  created: '2026-05-22'
  modified: '2026-05-22'
binds:
- namespace: env
  keys:
    AKAMAI_API_KEY: AKAMAI_API_KEY
capability:
  consumes:
  - type: http
    namespace: appsec-subscriptions
    baseUri: https://{hostname}/appsec/v1
    description: 'Akamai: Application Security API — Subscriptions business capability. Self-contained, no shared references.'
    resources:
    - name: configs-configId-notification-subscription-feature
      path: /configs/{configId}/notification/subscription/{feature}
      operations:
      - name: postpost-subscription-feature
        method: POST
        description: Subscribe or unsubscribe to recommendation emails
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: accountSwitchKey
          in: query
          type: string
          description: For customers who manage more than one account, this [runs the operation from another account](https://techdocs.akamai.com/developer/docs/manage-many-accounts-with-one-api-client).
            The Identity and
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: getget-subscription-feature
        method: GET
        description: List subscribers
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: accountSwitchKey
          in: query
          type: string
          description: For customers who manage more than one account, this [runs the operation from another account](https://techdocs.akamai.com/developer/docs/manage-many-accounts-with-one-api-client).
            The Identity and
