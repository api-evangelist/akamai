naftiko: 1.0.0-alpha2
info:
  label: 'Akamai: Application Security API — Hostname coverage'
  description: 'Akamai: Application Security API — Hostname coverage. 1 operations. Lead operation: Akamai Get hostname coverage. Self-contained Naftiko capability covering one Akamai
    business surface.'
  tags:
  - Akamai
  - Application Security
  - Hostname coverage
  created: '2026-05-22'
  modified: '2026-05-22'
binds:
- namespace: env
  keys:
    AKAMAI_API_KEY: AKAMAI_API_KEY
capability:
  consumes:
  - type: http
    namespace: appsec-hostname-coverage
    baseUri: https://{hostname}/appsec/v1
    description: 'Akamai: Application Security API — Hostname coverage business capability. Self-contained, no shared references.'
    resources:
    - name: hostname-coverage
      path: /hostname-coverage
      operations:
      - name: getget-hostname-coverage
        method: GET
        description: Get hostname coverage
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: accountSwitchKey
          in: query
          type: string
          description: For customers who manage more than one account, this [runs the operation from another account](https://techdocs.akamai.com/developer/docs/manage-many-accounts-with-one-api-client).
            The Identity and
