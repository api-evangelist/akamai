naftiko: 1.0.0-alpha2
info:
  label: 'Akamai: Property Manager API — Rule formats'
  description: 'Akamai: Property Manager API — Rule formats. 1 operations. Lead operation: Akamai List Rule Formats. Self-contained
    Naftiko capability covering one Akamai business surface.'
  tags:
  - Akamai
  - Rule formats
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AKAMAI_API_KEY: AKAMAI_API_KEY
capability:
  consumes:
  - type: http
    namespace: papi-rule-formats
    baseUri: https://{hostname}/papi/v1
    description: 'Akamai: Property Manager API — Rule formats business capability. Self-contained, no shared references.'
    resources:
    - name: rule-formats
      path: /rule-formats
      operations:
      - name: getruleformats
        method: GET
        description: Akamai List Rule Formats
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: PAPI-Use-Prefixes
          in: header
          type: string
          description: __Enum__ Sets whether to represent [ID prefixes](https://techdocs.akamai.com/property-mgr/reference/id-prefixes)
            in response data. Set to `false` when exchangin
          required: true
        - name: accountSwitchKey
          in: query
          type: string
          description: For customers who manage more than one account, this [runs the operation from another account](https://techdocs.akamai.com/developer/docs/manage-many-accounts-w
  exposes:
  - type: rest
    namespace: papi-rule-formats-rest
    port: 8080
    description: 'REST adapter for Akamai: Property Manager API — Rule formats. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.'
    resources:
    - path: /v1/rule-formats
      name: rule-formats
      description: REST surface for rule-formats.
      operations:
      - method: GET
        name: getruleformats
        description: Akamai List Rule Formats
        call: papi-rule-formats.getruleformats
        with:
          PAPI-Use-Prefixes: rest.PAPI-Use-Prefixes
          accountSwitchKey: rest.accountSwitchKey
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: papi-rule-formats-mcp
    port: 9090
    transport: http
    description: 'MCP adapter for Akamai: Property Manager API — Rule formats. One tool per consumed operation, routed inline
      through this capability''s consumes block.'
    tools:
    - name: akamai-list-rule-formats
      description: Akamai List Rule Formats
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: papi-rule-formats.getruleformats
      with:
        PAPI-Use-Prefixes: tools.PAPI-Use-Prefixes
        accountSwitchKey: tools.accountSwitchKey
      outputParameters:
      - type: object
        mapping: $.
