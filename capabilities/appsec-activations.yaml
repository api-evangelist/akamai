naftiko: 1.0.0-alpha2
info:
  label: 'Akamai: Application Security API — Activations'
  description: 'Akamai: Application Security API — Activations. 1 operations. Lead operation: Akamai Activate a configuration version. Self-contained Naftiko capability covering one
    Akamai business surface.'
  tags:
  - Akamai
  - Application Security
  - Activations
  created: '2026-05-22'
  modified: '2026-05-22'
binds:
- namespace: env
  keys:
    AKAMAI_API_KEY: AKAMAI_API_KEY
capability:
  consumes:
  - type: http
    namespace: appsec-activations
    baseUri: https://{hostname}/appsec/v1
    description: 'Akamai: Application Security API — Activations business capability. Self-contained, no shared references.'
    resources:
    - name: activations
      path: /activations
      operations:
      - name: postpost-activations
        method: POST
        description: Activate a configuration version
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: accountSwitchKey
          in: query
          type: string
          description: For customers who manage more than one account, this [runs the operation from another account](https://techdocs.akamai.com/developer/docs/manage-many-accounts-with-one-api-client).
            The Identity and
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
